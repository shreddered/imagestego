addons:
  apt:
    packages:
      - libopencv-dev
      - cmake-data
      - cmake
  homebrew:
    packages:
      - opencv
      - cmake

language: cpp

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - ctest
